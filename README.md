## Usage
1. Install [`uv`](https://docs.astral.sh/uv/#installation)
2. `uv venv && uv sync && . .venv/bin/activate` (Linux)
3. `python [script]`

The scripts generate figures, export data, and print some additional information to the console.
